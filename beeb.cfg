MEMORY {
	ZP:   start = $000000, size = $000100;
	RAM:  start = $000100, size = $00ff00;
}

SEGMENTS {
	ZEROPAGE: load = ZP,  type = bss, define = yes;
	BSS:      load = RAM, type = bss, define = yes;
	CODE:     load = RAM, type = rw;
}
