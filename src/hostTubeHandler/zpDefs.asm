ZP_TUBE_CLAIM_ID
	;	Boot needs to know this address when it's
	;	installing the host Tube handler
.export ZP_TUBE_CLAIM_ID
	db	0

ZP_TUBE_CONTROL_BLOCK_POINTER
	dw	0


ZP_HOST_TUBE_END
.export ZP_HOST_TUBE_END
